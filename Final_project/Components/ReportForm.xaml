<UserControl x:Class="Final_project.Components.ReportForm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
               mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>


        <Grid Grid.Row="0" Grid.IsSharedSizeScope="True">
            
            <!-- Row definitions -->
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

         
            <!-- The Tittle -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" SharedSizeGroup="lab" />
                    <ColumnDefinition Width="Auto" SharedSizeGroup="lab"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"  Text="Tittle" FontWeight="Bold" />
                <TextBlock Grid.Column="1" FontWeight="Bold" Text= "{Binding Tittle}" />

            </Grid>


            <!-- The client -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" SharedSizeGroup="lab" />
                    <ColumnDefinition Width="Auto" SharedSizeGroup="lab"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"  Text="Tittle" FontWeight="Bold" />
                <TextBlock Grid.Column="1" FontWeight="Bold" Text= "{Binding Kunde}" />

            </Grid>

            <!-- Status of the report -->
            <Grid Grid.Row="1" Margin="0 20 0 0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" SharedSizeGroup="lab"/>
                    <ColumnDefinition Width="Auto" SharedSizeGroup="lab"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" FontWeight="Bold" Text="Status" />
                <CheckBox Grid.Column="1" IsChecked="{Binding Status}" />
            </Grid>
        </Grid>

        <WrapPanel Grid.Row="1" Margin="0 20 0 0">
            <Button Margin ="0 0 10 0" Command="{Binding SubmitCommand}" Content="Submit"/>
            <Button Margin ="0 0 10 0" Command="{Binding CancelCommand}" Content="Cancel" Style="{StaticResource ButtonSecondary}"/>
        </WrapPanel>

     
    </Grid>
</UserControl>
