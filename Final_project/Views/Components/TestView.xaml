<UserControl x:Class="Final_project.Views.Components.TestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Final_project.Views.Components"
             xmlns:icon="clr-namespace:MahApps.Metro.IconPacks;assembly=MahApps.Metro.IconPacks.Material" xmlns:reportcomponentsvm="clr-namespace:Final_project.ViewModels.ReportComponentsVM" xmlns:components="clr-namespace:Final_project.Components" d:DataContext="{d:DesignInstance Type=reportcomponentsvm:TestVM}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <components:TextBoxView Grid.Column="0" Text="{Binding Name}" Watermark="
Tester utført ihht:"  Margin="0,0,10,0"/>

        <Button Grid.Column="1" Command="{Binding SubmitCommand}" Margin="0,20,10,0">
                   <icon:PackIconMaterial Kind="send" Height="20" Width="20" Foreground="{StaticResource IconColor1}"/>
          
        </Button>
    </Grid>
</UserControl>
